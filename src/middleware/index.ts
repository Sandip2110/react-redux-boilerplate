import React from 'react';
import axios from "axios";

export default async function dispatch(dispatch: any, action: string, body: any) {
    // let a: any = await axios.get("http://www.mocky.io/v2/5e11e948310000cb4d593f93");
    // dispatch({type: action, payload: a.name});
    // Will implement in the next pr
}
